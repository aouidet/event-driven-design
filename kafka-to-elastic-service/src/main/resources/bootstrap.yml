spring:
  application:
    name: kafka-to-elastic-service
  profiles:
    active: kafka-to-elastic
  config:
    enable: false
  cloud:
    config:
      name: config-client-kafka-to-elastic, config-client
      uri: http://localhost:8888
      username: ${token_config_server_user}
      password: ${token_config_server_password}